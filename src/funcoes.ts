// FUNÇÃO MUÇTIPLICAÇÃO
const multiplicacao = (fator1:number, fator2:number): number => (fator1 * fator2);
const resultadoMultiplica = multiplicacao(10, 10);
console.log(resultadoMultiplica);

// FUNÇÃO SAUDAÇÃO
const saudacao = (nome: string): string => `Olá, ${nome}!`;
const resultadoSaudacao = saudacao('Cláudio');
console.log(resultadoSaudacao);